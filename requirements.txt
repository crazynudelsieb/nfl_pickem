Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-SocketIO==5.6.1
Flask-WTF==1.3.0
Flask-Caching==2.4.1
redis==7.1.0
Flask-Migrate==4.1.0
WTForms==3.2.2
Werkzeug==3.1.8
psycopg[binary,pool]==3.3.4
python-socketio==5.16.3
python-engineio==4.13.3
# WebSocket transport for async_mode="threading" - no monkey patching required.
simple-websocket==1.1.0
requests==2.34.2
python-dotenv==1.2.2
pytz==2026.2
APScheduler==3.11.3
email-validator==2.3.0
gunicorn==26.0.0
# The [redis] extra is load-bearing, not cosmetic: it carries limits' redis<8
# cap into the resolver. Without it an unsafe redis bump installs cleanly and
# fails at runtime instead of failing loudly at install time.
Flask-Limiter[redis]==4.1.1
